<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- linear layout: layout width/height, orientation, gravity -->
    <style name="form_layout">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:orientation">vertical</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- wrap content dimensions -->
    <style name="wrapContentBase">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">@dimen/fab_margin</item>
    </style>

    <!-- width fields -->
    <style name="widthFieldBase" parent="wrapContentBase">
        <item name="android:width">200dp</item>
    </style>

    <!-- TextView fields -->
    <style name="textView" parent="wrapContentBase">
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:textSize">20dp</item>
    </style>

    <!-- TextView message -->
    <style name="textViewMessage" parent="textView">
        <item name="android:textColor">@color/colorText</item>
    </style>

    <!-- EditText form -->
    <style name="editText" parent="widthFieldBase">
        <item name="android:textColorHint">#84d3b3</item>
    </style>

    <!-- Button join form -->
    <style name="buttonJoin" parent="widthFieldBase">
        <item name="android:background">@color/colorAccent</item>
        <item name="android:textColor">#fff</item>
    </style>

    <!-- Button clean field -->
    <style name="buttonClean" parent="widthFieldBase">
        <item name="android:background">#fff</item>
        <item name="android:textColor">#888</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
